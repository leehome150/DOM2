## (部分)使用示例：

```
$('<div><.div>')//创建一个div

$('div').get(2).addClass('red')//查找index为2的div添加red样式

$('div').append('<span>test</span>')//把span放在div元素的内部的后面

$('div').find('.red').end().find('.blue')//在div元素里面找到样式为blue 的元素

···
```
